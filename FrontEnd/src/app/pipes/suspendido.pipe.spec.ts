import { SuspendidoPipe } from './suspendido.pipe';

describe('SuspendidoPipe', () => {
  it('create an instance', () => {
    const pipe = new SuspendidoPipe();
    expect(pipe).toBeTruthy();
  });
});
